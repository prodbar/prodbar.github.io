# Dataset

The dataset used in this project was:

```text
GenZ_dataset_con_low.xlsx
```

The dataset is kept local in this folder. The portfolio repository should not publish raw data unless publication permission is explicitly confirmed.

The analysis notebook expects the dataset to be placed in this `data/` folder.
